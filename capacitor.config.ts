import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.danasugu.cistourro',
  appName: 'CisTourRo',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
